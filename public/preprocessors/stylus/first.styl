/* 
En los estilos debe separarse los colores de los tamaños. 
Se diseña con atomic design.
Primitivos
    Colores
    tamaños
    Espaciados
Atomos
    botones
    Cajas de texto
    Inputs
    Iconos
Organismos
    Elementos de interfaz que se pueden reusar.
    Barra de busqueda con botones
Molecular o cards
    Formularios
    Barra de navegación principal
Secciones o templastes
    Heder
    Seciones o pestañas de una página
    Footer
Paginas
    Inicio
    Carga de archivos
    Panel administrativo
    Panel de perfil
*/

/* Este css se debe compilar en la carpeta css de public */
:root 
    /* Colores */
    --siraq-gris-fondo #111E27
    --texto-botones #999A9A
    --verde-botones #02AF9C
    --plataforma-de-fondo #202D36
    --celeste-comun #35A9F2
    --color-agua-sistema aqua
    --color-background-input #2B383E
    --color-font-input #D7D7E1
    /* Tipografía */
    --font-size-common 'Roboto', sans-serif
    /* Tamaños */
    --ancho-nav 850px
    --ancho-wrapper 750px
*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: sans-serif;
}


common_text()
    text-decoration none
    font-size 0.8em
    font-family var(--font-size-common)
    color var(--texto-botones)

width_section(var_width)
    margin-left auto
    margin-right auto
    max-width var_width


.wrapper_admin
    display grid
    grid-template-columns 1fr 6fr
    background-color #111E27

.color_main_button
    background-color var(--verde-botones)
    color white
    border none
    font-family: var(--font-size-common);
    
.common_form_inputs
    background-color: var(--color-background-input);
    font-family: var(--font-size-common);
    color: var(--color-font-input);

@require 'main_page'
/* Panel administrativo */

.login_section {
    display: grid;
    grid-template-rows: 1fr 2em 18em 2em 1.5fr;
    grid-template-columns: 1fr 5fr 1fr;
    font-size: 1em;
    grid-row-gap: 4em;
    background-color: rgba(17, 30, 39, 1);
    height: 100vh;
}

.title_login {
    grid-column-start: 2;
    text-align: center;
    display: grid;
    align-items: center;
    grid-area: 2/2;
}

.title_login h1 {
    margin: 0;
    font-size: 2em;
    font-family: var(--font-size-common);
    color: #C4C4C4;
}

.login_section form {
    grid-area: 3 / 2;
    display: grid;
    grid-template-rows: 1fr 1fr 0.5fr 1fr;
    grid-gap: 1em;
    font-size: 2em;
}

.login_section form div {
    border-radius: 2em; 
    padding-left: 1em; 
    padding-right: 1em; 
    font-size: 0.65em; 
    color: rgba(215, 215, 225, 1); 
    display: flex; 
    align-items: center;
}

.login_section form div input {
    height: 1.4em;
    width: 1.4em;
}

.login_section form div label {
    font-size: 0.7em;
    font-family: var(--font-size-common);
    color: rgba(53, 169, 242, 1);
    margin-left: 0.5em;

}

.login_section form .element {
    border-radius: 2em;
    padding-left: 2em;
    padding-right: 2em;
    font-size: 0.65em;
    color: rgba(215, 215, 225, 1);
    border: none;

}

.inputs_login {
    background-color: var(--plataforma-de-fondo);
}

.opciones_login {
    text-align: center;
    grid-area: 4 / 2; 
    display: grid; 
    grid-template-columns: 1fr 1fr; 
}

.opciones_login a{
    color: var(--celeste-comun);
    font-size: 1.2em;
    font-family: var(--font-size-common);
    align-items: center; 
    justify-content: center;

}

@require 'administrative_panel'

